# firebase

This folder will contain Firebase configurations, initialization logic, and wrappers for Firebase services.

Examples:
- `config.js`: Firebase SDK initialization and export of Auth, Firestore, and Realtime Database references.
- `db.js`: Firestore database helper operations (CRUD for medicines, users, schedules).
- `auth.js`: Authentication functions (sign in, sign out, user registration).
