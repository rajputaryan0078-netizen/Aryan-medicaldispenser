# services

This folder is for external service integrations, API calls, state sync logic, or hardware communication interfaces.

Examples:
- `iotApi.js`: Handles REST/WebSocket communication with the physical IoT dispenser hardware.
- `notificationService.js`: Manages alerts for low inventory, missed doses, or dispensing errors.
