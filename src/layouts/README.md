# layouts

This folder contains page layout templates to organize the layout structure (headers, sidebars, footers) for the app.

Examples:
- `DashboardLayout.jsx`: The layout shell containing the Sidebar and Header wrapper that surrounds dashboard pages.
- `AuthLayout.jsx`: Centered single-column layout for login and signup pages.
