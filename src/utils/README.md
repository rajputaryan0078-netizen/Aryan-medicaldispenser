# utils

This folder contains utility helper functions, formatting scripts, and constant definitions that are reusable throughout the app.

Examples:
- `dateFormatter.js`: Formats timestamps from IoT dispensing events.
- `validators.js`: Validates medicine dosages and timing schedules.
- `constants.js`: System configuration constants (status levels, time periods).
