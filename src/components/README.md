# components

This folder is dedicated to reusable presentational and UI components that are shared across different pages of the application.

Examples:
- Common UI: Button, Input, Modal, Card, Table, Sidebar, Header
- Medical Dispenser UI: MedicineCard, StatusBadge, DispenseHistoryTable, InventoryProgressChart
